import 'package:flutter/material.dart';

class ChampionInfoFirst extends StatelessWidget {
  const ChampionInfoFirst({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
